@model IEnumerable<PiniT.Models.RestaurantType>

@{
    ViewBag.Title = "RestaurantTypeIndex";
}

<h2 class="a text-center mx-auto mt-3 mb-3">RestaurantTypeIndex</h2>

<p>
    
</p>
<table class="col-md-6 font-weight-normal mx-auto text-center container bg-white">
    <tr>
        <th class="h4">
            @Html.DisplayNameFor(x => x.Name)
           
        </th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td class="font-italic">
        @Html.DisplayFor(modelItem => item.Name)
    </td>
    <td class="form-group mx-auto text-center btn-group">
    @Html.ActionLink(" ", "CreateRestaurantType", null, new { @class = "btn btn-primary fas fa-plus" })
    </td>
</tr>
}

</table>
